/**
 * read Blob or File
 * @param {*} params
 * @return {String} url
 * readyState、onload
 * dataUrl(base64)
 */
function fileReader() {
  // const reader = new FileReader();
}

export default fileReader;
